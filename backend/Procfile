# Límite de workers = límite de conexiones a PostgreSQL.
# 1 worker para Clever Cloud (~5 conexiones) y evitar "too many connections".
web: gunicorn -w 1 -b 0.0.0.0:$PORT --timeout 120 neiraestudio.wsgi:application
